<h1>You've made it this far, might as well carry on.</h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      <a href="http://<%= link.url %>"><%= link.title %></a>
    </li>
  <% end %>
</ul>
